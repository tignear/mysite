language: node_js
node_js:
  - "8"

cache:
  directories:
    - "node_modules"

branches:
  only:
  - master

install:
  - yarn 
  - yarn run build

script:
  - echo "Skipping tests"

deploy:
  provider: azure_web_apps